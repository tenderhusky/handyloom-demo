/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // This ensures your JSON data is accessible during the build
  serverExternalPackages: [], 
};

export default nextConfig;
